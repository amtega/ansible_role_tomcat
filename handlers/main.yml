---
# Role handlers

- name: Tomcat restart service
  service:
    name: "{{ tomcat_name }}"
    state: restarted
    enabled: yes
  listen: restart tomcat
  tags:
    - role::tomcat
    - role::tomcat::restart
