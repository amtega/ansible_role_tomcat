---
# Role install tasks

- name: setup system group
  group:
    name: "{{ tomcat_group }}"
    gid: "{{ tomcat_group_gid | default(omit) }}"
  tags:
    - role::tomcat
    - role::tomcat::install
    - role::tomcat::install::groups
