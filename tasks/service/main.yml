---
# Install tasks

- block:
    - import_tasks: "{{ role_path }}/tasks/service/systemd.yml"
    - import_tasks: "{{ role_path }}/tasks/service/systemv.yml"
    - import_tasks: "{{ role_path }}/tasks/service/instances.yml"
  tags:
    - role::tomcat
    - role::tomcat::service
