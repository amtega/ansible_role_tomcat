---
# Role default lower priority variables

# List of tomcat instances to create

tomcat_instances:
  - name: server
    version: 8.0.9
#    link: /opt/tomcat               # Where to put a soft link to the base
#    download_url:                  # Use this variable to use an sepecific url
#                                   # to the redistributable tarball. Keep in
#                                   # mind that if you use this variable
#                                   # instance version variable must match
#                                   # with tarball filename

# Default instance options to apply if no other ones are specified in the
# specific instance dictionary of the tomcat_instances variable.

tomcat_instance_base: /opt
tomcat_instance_home: /srv/tomcat
tomcat_instance_user: tomcat
tomcat_instance_group: tomcat
tomcat_instance_server_port: 8005
tomcat_instance_server_http_port: 8080
tomcat_instance_server_https_port: 8443
tomcat_instance_server_ajp_port: 8009
tomcat_instance_java_home: /usr/lib/jvm/jre
tomcat_instance_java_opts: -Xmx1024m -Xms1024m
tomcat_instance_download_mirror: http://archive.apache.org
tomcat_instance_download_path: /tmp
tomcat_instance_remove_download: true


# Where to download tomcat redistributable
# Remove the tomcat redistributable after install



# Tomcat instances configuration

#
# Proxy settings
#

# IP address and port, or URL, for using an http_proxy.

#tomcat_http_proxy:

# IP address and port, or URL, for using an https_proxy.

#tomcat_https_proxy:

# Exclude IP address or URL from the proxy.

#tomcat_no_proxy:
